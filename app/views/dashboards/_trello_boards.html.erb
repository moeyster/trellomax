<div class="card mb-4">
  <div class="card-block">
    <h3 class="card-title">My Trello Boards</h3>
    <h6 class="card-subtitle mb-2 text-muted">Most recently active</h6>

    <% if current_user.boards.present? %>
      <% current_user.boards.each do |board| %>
        <div class="row">
          <div class="col-md-9">
            <h5 class="pt-1"><%= board.name %></h6>
            <p>
              <%= board.date_last_activity.strftime("%r %d %B %Y") %>
            </p>
          </div>
          <div class="col-md-2">
            <%= link_to "Export", root_path %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
